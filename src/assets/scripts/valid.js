export default{
    //正規表達式
    Regex:{
        //使用者名稱，英文大寫+英數混和共4字元
        Username:/^[A-Z]+(?=.*[A-Za-z0-9]{3,}$)/,
        //密碼，至少1大寫字母1小寫字母1數字，4-8字元
        Password:/^(?=.*\d)(?=.*[A-Z])(?=.*[a-z]).{4,8}$/,
    }
}